<b>howdy</b>
<br>
<%= "Size: #{params[:id]}"%>
<br>

<% Locker.where(size: params[:id]).each do |lock| %>
    <%= "Locker Number: #{lock.number}" %>
    <%= "Student: #{lock.student}"%>
    <br>
<%end%>
<br><br><br><br>
add filters